# dependencies
node_modules/

# environment files — never commit credentials
.env*
!.env.example

# build output
dist/

# misc
.DS_Store
*.log

# Screenshot output from `npm run shots` — regenerate, never commit.
shots/
